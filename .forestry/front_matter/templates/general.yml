---
label: General
hide_body: true
fields:
- name: icon
  type: file
  config:
    maxSize: 250
  label: Icon
  description: Icon used as favicon and pwa icon
- name: site_title
  type: text
  config:
    required: false
  label: Site title
  description: Used as page title (displayed in tab)
