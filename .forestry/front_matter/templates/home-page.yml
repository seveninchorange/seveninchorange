---
label: Home page
hide_body: true
fields:
- name: header_title
  type: text
  config:
    required: false
  label: Header Title
- name: header_image
  type: file
  config:
    maxSize: 250
  label: Header image
- name: logo
  type: file
  config:
    maxSize: 250
  label: Logo
pages:
- setup/front-page.json
